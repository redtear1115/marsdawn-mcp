{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://marsdawn.southern-light.dev/schemas/cli/export.v1.json",
  "title": "marsdawn export --json: success",
  "description": "Printed on stdout as one line when `marsdawn export --json` succeeds (exit code 0).",
  "type": "object",
  "required": [
    "ok",
    "output",
    "pages",
    "theme",
    "paper",
    "diagramErrors"
  ],
  "additionalProperties": false,
  "properties": {
    "ok": {
      "const": true
    },
    "output": {
      "type": "string",
      "description": "Absolute path of the PDF that was written."
    },
    "pages": {
      "type": "integer",
      "minimum": 0,
      "description": "Number of pages in the PDF."
    },
    "theme": {
      "enum": [
        "dawn",
        "classic",
        "modern",
        "vivid"
      ],
      "description": "Theme used for the export."
    },
    "paper": {
      "enum": [
        "a4",
        "letter"
      ],
      "description": "Paper size used for the export."
    },
    "diagramErrors": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "One message per Mermaid diagram that failed to render. The PDF is still written."
    },
    "diagramErrorDetails": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "message"
        ],
        "additionalProperties": false,
        "properties": {
          "message": {
            "type": "string",
            "description": "The error message, same text as the matching entry in diagramErrors."
          },
          "fenceLine": {
            "type": "integer",
            "minimum": 1,
            "description": "The document line the Mermaid fence starts on. Absent only for fences inside footnotes."
          },
          "line": {
            "type": "integer",
            "minimum": 1,
            "description": "The error's own document line (fenceLine plus Mermaid's line). Present only when Mermaid's message names a line."
          }
        }
      },
      "description": "One entry per diagramErrors entry, in the same order, with the document line each error is on when known."
    }
  }
}
