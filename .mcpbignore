# Not part of the bundle: tests, tooling and repository files. Anchored, so
# directories of the same name inside node_modules are left alone.
/test/
/scripts/
/.github/
/.gitignore
/.mcpbignore
/CONTRIBUTING.md
/SECURITY.md
*.mcpb
# The registry entry describes the bundle; it is not part of it.
/server.json
